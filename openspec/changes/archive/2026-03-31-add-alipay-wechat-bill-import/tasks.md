## 1. 数据层

- [x] 1.1 为 `transactions` 增加 `import_platform`、`external_id` 及账单明细列 `bill_category`、`bill_counterparty`、`bill_product`、`bill_payment_method`、`bill_merchant_no`、`bill_export_note`（均为可空）；增加 `(user_id, import_platform, external_id)` 唯一约束（实现时处理 NULL 语义，如部分索引）
- [x] 1.2 扩展列表/详情 API 与记账页展示：对手工记录不显示空列；导入记录展示账单字段（可折叠/次要样式以避免挤占主列表）
- [x] 1.3 确认现有写入路径（手工录入）对上述列保持 `NULL` 或不误填

## 2. 解析与 API

- [x] 2.1 实现支付宝 CSV 解析：定位表头行、过滤说明行、跳过「不计收支」、状态过滤、金额与日期解析
- [x] 2.2 实现微信 XLSX 解析：定位表头行、跳过「中性交易」、处理 Excel 日期序列、状态过滤
- [x] 2.3 新增已认证导入端点：参数含平台枚举 + 上传文件；校验扩展名与魔数/表头与所选平台一致，否则 400
- [x] 2.4 批量插入与去重：写入 `bill_*` 列与幂等字段；返回摘要（新建条数、跳过条数、失败原因）

## 3. 前端（记账页）

- [x] 3.1 增加「导入」控件：下拉选择支付宝 / 微信 + 文件选择 + 提交
- [x] 3.2 展示导入结果反馈（成功摘要或错误文案），并刷新列表与汇总

## 4. 依赖与验证

- [x] 4.1 `requirements.txt` 增加 XLSX 解析依赖并锁定下限版本
- [x] 4.2 使用 `账单/` 下样例或脱敏副本编写解析与导入集成测试；覆盖未登录 401、平台不匹配、去重

**依赖**：1.x → 2.x → 3.x；4.x 可与 2.x 并行。
