# 实施任务：`add-personal-ledger-web`

## 1. 基础与数据层

- [x] 1.1 选定运行时与 Web 框架，初始化项目依赖与入口（单命令启动）。
- [x] 1.2 定义 SQLite 库文件路径（例如项目根或 `data/`），启动时创建表并建立索引（按 `id`、按 `created_at`）。
- [x] 1.3 实现交易 CRUD 与汇总查询 API：创建（消费/收入+金额+可选补充信息）、列表、更新补充信息/金额（若规范需要）、删除（可选，若规范未要求可省略或标记为未来项）。

## 2. 前端界面

- [x] 2.1 实现录入表单：类型切换、金额输入、补充信息输入；提交后列表与汇总更新。
- [x] 2.2 实现汇总区：累计消费、累计收入、净值；与接口数据一致。
- [x] 2.3 实现交易列表：展示类型、金额、时间、补充信息摘要；支持展开或侧栏编辑补充信息。
- [x] 2.4 应用「简洁 + 现代」视觉样式（配色、卡片、响应式基本可用）。

## 3. 验证与交付

- [x] 3.1 手动验证：录入多笔消费与收入、刷新页面后数据仍存在、汇总正确。
- [x] 3.2 补充 README 或运行说明：如何安装依赖、启动服务、数据库文件位置（不写冗长文档，仅足够自用）。
- [x] 3.3（可选）为核心 API 或数据层编写最小自动化测试。

**依赖**：1.x 完成后 2.x 可并行对接 mock；建议顺序 1.1 → 1.2 → 1.3 → 2.x → 3.x。
