## 1. 分页

- [x] 1.1 后端：`GET /api/transactions` 增加 `page`（默认 1）和 `page_size`（默认 50）参数
- [x] 1.2 后端：返回结构变为 `{ items: [...], total: N, page: N, page_size: N }`
- [x] 1.3 前端：实现滚动加载或「加载更多」按钮
- [x] 1.4 编写测试：验证分页参数与返回结构

## 2. SQLite WAL 模式

- [x] 2.1 在 `get_conn()` 中添加 `PRAGMA journal_mode=WAL`
- [x] 2.2 验证并发读写场景下的正确性

## 3. 加载指示器

- [x] 3.1 定义统一的加载状态管理（全局 loading flag 或按操作粒度）
- [x] 3.2 登录、记账、导入、刷新列表等操作期间禁用提交按钮并显示 spinner
- [x] 3.3 添加顶部进度条或内联 spinner 样式

## 4. Toast 可关闭

- [x] 4.1 为 Toast 添加关闭按钮或点击关闭行为
- [x] 4.2 关闭后清除自动消失定时器

## 5. 交易编辑增强

- [x] 5.1 后端：`TransactionPatch` 模型增加 `type` 和 `transacted_on` 可选字段
- [x] 5.2 后端：PATCH 接口逻辑处理类型与日期的更新
- [x] 5.3 前端：交易详情展开区显示类型和日期的编辑控件
- [x] 5.4 编写测试：验证类型和日期修改

## 6. 数据导出

- [x] 6.1 后端：新增 `GET /api/transactions/export` 接口，返回 CSV（支持筛选参数）
- [x] 6.2 前端：在汇总操作区增加「导出 CSV」按钮
- [x] 6.3 编写测试：验证导出内容与格式

## 7. 交易搜索

- [x] 7.1 后端：`GET /api/transactions` 增加 `keyword` 参数，对 `note`、`bill_counterparty`、`bill_product` 做 LIKE 模糊匹配
- [x] 7.2 前端：在流水列表上方添加搜索输入框，输入后触发筛选请求
- [x] 7.3 编写测试：验证关键词搜索返回正确结果

## 8. 用户自助修改密码

- [x] 8.1 后端：新增 `POST /api/me/password` 接口，需验证旧密码后设置新密码
- [x] 8.2 前端：在用户信息区添加「修改密码」入口与表单
- [x] 8.3 编写测试：验证旧密码错误时拒绝、正确时更新

## 9. 管理员删除用户

- [x] 9.1 后端：新增 `DELETE /api/admin/users/{user_id}` 接口，删除用户及其所有交易和会话
- [x] 9.2 禁止管理员删除自身
- [x] 9.3 前端：用户列表增加「删除」按钮并弹出确认框
- [x] 9.4 编写测试：验证删除行为与自删禁止
