# 实施任务：`add-multi-user-and-admin`

## 1. 数据模型与迁移

- [x] 1.1 设计并创建 `users`、会话存储（表或等价机制）；为 `transactions` 增加 `user_id`、外键与索引（含 `(user_id, created_at)`）。
- [x] 1.2 编写启动时 schema 迁移：兼容空库与已有 `ledger.db`；对无 `user_id` 的旧数据定义归属策略并文档化。
- [x] 1.3 实现密码哈希与校验工具函数；禁止记录明文口令。

## 2. 认证与管理 API

- [x] 2.1 实现登录、登出、`GET /api/me`（或等价）返回当前用户与角色；失败返回适当 HTTP 状态。
- [x] 2.2 将所有原收支 API 改为**需登录**，并在服务端强制 `user_id = 当前用户`。
- [x] 2.3 实现管理员 API：用户列表、创建用户、按规范支持的禁用或删除；所有写操作校验 `admin` 角色。（另：管理员可重置密码，见设计补充。）
- [x] 2.4 实现首轮管理员引导（环境变量或无用户时创建），与「无管理员且无法引导」时的失败策略。

## 3. 前端

- [x] 3.1 普通用户：登录/登出 UI；登录后可访问现有记账交互；请求携带 Cookie（`credentials: 'include'`）。
- [x] 3.2 管理端页面：用户列表、创建用户、禁用或删除（与规范一致）；非管理员访问时拒绝或提示无权限。
- [x] 3.3 确认跨用户隔离：用两个浏览器会话手工验证 A 看不到 B 的流水与汇总。（自动化：`test_two_users_isolated`；可再行手工复验。）

## 4. 验证与文档

- [x] 4.1 自动化测试：登录、401 未登录访问收支、两用户数据隔离、管理员与普通用户接口权限。
- [x] 4.2 更新 `README`：环境变量、管理员初始化、多用户说明。

**依赖**：1.x → 2.x → 3.x → 4.x；2.2 依赖 2.1。
