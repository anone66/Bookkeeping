## 修改需求

### 需求：受保护的收支访问控制

系统必须禁止**未认证**客户端访问创建、查询、修改或删除**本人以外或匿名**的收支数据的所有接口；此类请求必须返回未认证错误且不得在响应体中泄露其他用户的存在性或流水内容。

#### 场景：未登录不可记账

- **当** 客户端在未携带有效会话的情况下调用收支写入接口
- **那么** 请求被拒绝且不写入数据库

#### 场景：未登录不可读汇总与列表

- **当** 客户端在未携带有效会话的情况下调用收支汇总或列表接口
- **那么** 请求被拒绝

#### 场景：未登录不可导入账单

- **当** 客户端在未携带有效会话的情况下调用账单批量导入接口
- **那么** 请求被拒绝且不写入数据库
